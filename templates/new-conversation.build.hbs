<h4>New conversation</h4>

<p>Select contact to start a conversation</p>

<ul class="contacts">
  {{#each contacts}}
    <li>
      <a href="#" data-create-conversation="{{ id }}">{{ name }}</a>
    </li>
  {{/each}}
</ul>