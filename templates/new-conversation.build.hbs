<h4>New conversation</h4>

<p>Select contact</p>

<ul class="contacts">
  {{#each contacts}}
    <li>
      <a href="#" data-create-conversation="{{ id }}">{{ fullName }}</a>
    </li>
  {{/each}}
</ul>